[project]
name = "anime-sub-ai"
version = "0.1.0"
description = "Local-first anime subtitle AI pipeline MVP"
requires-python = ">=3.12"
dependencies = [
  "openai>=1.80.0",
  "pysubs2>=1.8.0",
  "PyYAML>=6.0.2",
  "typer>=0.12.5",
]

[project.scripts]
anime-sub = "app.cli.main:app"

